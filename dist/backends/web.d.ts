import type { HapticBackend } from "../types.js";
/** The pure-web haptic backend: Android `navigator.vibrate` + the iOS switch tick. */
export declare function createWebBackend(): HapticBackend;
//# sourceMappingURL=web.d.ts.map